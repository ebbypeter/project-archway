# Views for IdentityCloud — kept next to the system definition.

systemContext identityCloud "IdentityCloud-Context" "The authentication landscape: every system and person that depends on IdentityCloud." {
    include *
    autoLayout lr
}
