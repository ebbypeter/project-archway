# Views for GridView — kept next to the system definition.
# Included by the workspace views block. Change autoLayout (lr/tb) to taste.

systemContext gridView "GridView-Context" "GridView and its direct dependencies." {
    include *
    autoLayout lr
}
