== Trust Views

The external trust surface: only the `VendorHosted`/`SaaS`-tagged systems.
Vendor-hosted systems have dashed borders per the shared styles. The
authentication landscape — everything that depends on IdentityCloud — is the
context view on the IdentityCloud system page.

image::embed:VendorHostedSystems[Vendor-Hosted Systems]

Authentication methods and data classifications for each interface are recorded
in the integration documentation under `models/integrations/`.
