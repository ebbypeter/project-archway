== Enterprise Landscape

The complete landscape of Apex Energy Corporation. System-scoped views
(context, container, component, deployment) are on each system's page under
_Software Systems_.

=== Reading the diagrams

Fill colour is the *portfolio*; the border describes hosting and criticality.

++++
<table style="border-collapse:collapse;margin:1rem 0;">
  <tr>
    <td style="padding:.35rem .6rem;"><span class="portfolio critical"></span></td>
    <td style="padding:.35rem .6rem;">Critical Apps</td>
    <td style="padding:.35rem .6rem;"><span class="portfolio market"></span></td>
    <td style="padding:.35rem .6rem;">Market Apps</td>
  </tr>
  <tr>
    <td style="padding:.35rem .6rem;"><span class="portfolio enterprise"></span></td>
    <td style="padding:.35rem .6rem;">Enterprise Apps</td>
    <td style="padding:.35rem .6rem;"><span class="portfolio external"></span></td>
    <td style="padding:.35rem .6rem;">External Apps</td>
  </tr>
  <tr>
    <td style="padding:.35rem .6rem;"><span style="display:inline-block;width:2.2rem;height:1.1rem;background:transparent;border:3px solid #1a1a1a;border-radius:3px;vertical-align:middle;"></span></td>
    <td style="padding:.35rem .6rem;">Business-critical</td>
    <td style="padding:.35rem .6rem;"><span style="display:inline-block;width:2.2rem;height:1.1rem;background:transparent;border:3px dashed #1a1a1a;border-radius:3px;vertical-align:middle;"></span></td>
    <td style="padding:.35rem .6rem;">Vendor-hosted</td>
  </tr>
</table>
++++

Systems of the same portfolio are also drawn inside a labelled boundary box.

// The tab strip below replicates the markup of the native per-system page
// tabs (Info / Context views / ...), so both landscape variants present the
// same way — but toggled in place, since this is a single documentation page.
++++
<div class="tabs mt-3">
  <ul id="landscape-tabs" class="is-flex-wrap-wrap is-flex-shrink-1 is-flex-grow-0">
    <li class="is-active" data-target="landscape-with-people"><a class="is-active">With people</a></li>
    <li data-target="landscape-systems-only"><a>Systems only</a></li>
  </ul>
</div>
<div class="content p-3" id="landscape-with-people">
  <img src="embed:EnterpriseLandscape" alt="Enterprise Landscape">
</div>
<div class="content p-3" id="landscape-systems-only" style="display: none;">
  <img src="embed:SystemsOnlyLandscape" alt="Systems-Only Landscape">
</div>
<script>
  document.querySelectorAll('#landscape-tabs li').forEach(function (tab) {
    tab.addEventListener('click', function () {
      document.querySelectorAll('#landscape-tabs li').forEach(function (t) {
        t.classList.remove('is-active');
        t.querySelector('a').classList.remove('is-active');
        document.getElementById(t.dataset.target).style.display = 'none';
      });
      tab.classList.add('is-active');
      tab.querySelector('a').classList.add('is-active');
      document.getElementById(tab.dataset.target).style.display = '';
    });
  });
</script>
++++
