== Technology Workspace

*Audience:* Technology Leadership, Technology Lifecycle Managers.

This workspace answers estate questions from system metadata: what runtimes,
databases and hosting platforms are in use, and where.

Technology facts are stored as *properties* on each system (`runtime`,
`database`, `hosting`, `lifecycle`) — never as tags. The inventory reports in
this documentation are generated from those properties by the pipeline.
