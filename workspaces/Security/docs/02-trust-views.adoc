== Trust Views

=== Trust Landscape

The full estate; vendor-hosted systems have dashed borders and SaaS platforms
are coloured per the shared styles. The authentication landscape (everything
that depends on IdentityCloud) is on the IdentityCloud system page.

image::embed:TrustLandscape[Trust Landscape]

=== Vendor-Hosted Systems

Only the `VendorHosted`/`SaaS`-tagged systems — the external trust surface.

image::embed:VendorHostedSystems[Vendor-Hosted Systems]
